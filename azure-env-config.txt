# Azure App Service Environment Variables Configuration
# Copy these settings to Azure Portal -> App Service -> Configuration -> Application Settings

DATABASE_URL=sqlserver://doaibuadmin:gg7$5oEu7vkI90@doaibustore-server.database.windows.net:1433;database=doaibustore-db;encrypt=true;trustServerCertificate=false;connectionTimeout=30;requestTimeout=30;

JWT_SECRET=doaibu-store-jwt-secret-key-2024-very-secure-random-string

NEXTAUTH_SECRET=doaibu-store-nextauth-secret-2024-random-key

NEXTAUTH_URL=https://doaibustore.site

NODE_ENV=production

PORT=8080

WEBSITE_NODE_DEFAULT_VERSION=22.x

# Prisma configuration
PRISMA_CLI_BINARY_TARGETS=native,linux-musl 